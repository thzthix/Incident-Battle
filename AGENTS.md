# AGENTS.md

## 목적
Incident Battle는 시나리오 질문 대응을 짧고 반복 가능한 배틀 루프로 훈련하는 웹앱이다.
핵심은 포켓몬식 전투 리듬을 빌리되, 사용자가 상황을 읽고 우선순위를 정하고 답변 구조를 만드는 데 있다.

## 작업 방식
- 항상 작은 vertical slice 단위로 작업한다.
- UI, 상태머신, 시나리오 데이터, 채점 규칙을 한 번에 크게 바꾸지 않는다.
- 계약을 먼저 고정하고, 그다음 구현한다.
- 애매하면 기능을 늘리지 말고 더 작은 플레이어블로 줄인다.

## 구현 전 순서
아래 항목을 건드릴 때는 바로 구현하지 말고 먼저 짧은 spec을 만든다.
- 화면 구조
- 사용자 문구
- 상태 전이
- 시나리오 JSON 형태
- 채점/피드백 규칙
- OpenAI 사용 방식

순서:
1. 목표
2. 입력/출력 계약
3. 성공 기준
4. 영향 파일
5. 열린 질문
6. 승인
7. 구현

## 브랜치 규칙
- 기본 브랜치: `main`
- 기능 브랜치: `feat/...`
- 수정 브랜치: `fix/...`
- 문서 브랜치: `docs/...`
- 리팩터 브랜치: `refactor/...`

규칙:
- 한 브랜치에는 한 논리적 변경만 담는다.
- 브랜치 이름은 변경 목적이 드러나게 짓는다.
- self-check 없이 커밋하지 않는다.

## 커밋 규칙
- 커밋 메시지는 `영어 prefix + 한글 설명` 형식으로 쓴다.
- prefix는 `feat`, `fix`, `docs`, `refactor`, `chore`, `style`만 사용한다.
- 예시: `feat: 첫 플레이어블 전투 셸 구성`, `fix: 꼬리질문 상태 전이 수정`
- 커밋은 작고 되돌리기 쉽게 유지한다.
- 한 커밋에는 한 가지 의미 있는 변경만 담는다.
- 화면 변경과 데이터 변경이 동시에 크면 커밋을 분리한다.
- 빌드나 핵심 검증이 깨진 상태로 커밋하지 않는다.

## 프론트엔드 규칙
- 스택: `React + TypeScript + Vite`
- 상태관리: `useReducer` 우선
- 스타일: `plain CSS + CSS variables`
- 애니메이션: `CSS transition/keyframes` 우선
- battle flow는 reducer/state machine에서 명시적으로 관리한다.
- 시나리오 데이터와 채점 규칙은 UI 컴포넌트에 하드코딩하지 않는다.

## 네이밍 규칙
- 컴포넌트: `PascalCase`
- 훅: `useSomething`
- 유틸 함수: `camelCase`
- reducer action: `UPPER_SNAKE_CASE` 또는 고정 상수형 문자열
- 타입/인터페이스: `PascalCase`

## 컴포넌트 규칙
- 컨테이너와 프레젠테이셔널 컴포넌트를 분리한다.
- `BattleFlowPage`가 오케스트레이션하고, 하위 컴포넌트는 화면 조각만 책임진다.
- 점수 계산, 상태 갱신, 시나리오 선택은 순수 함수로 분리한다.
- 거대한 단일 컴포넌트에 전투 로직, 데이터 로딩, 스타일을 모두 몰아넣지 않는다.

## UX 규칙
- DPPT풍 문법은 자산 복제가 아니라 리듬과 흐름으로 가져온다.
- 긴 사건 설명은 한 번에 던지지 말고 순차 reveal로 보여준다.
- `전장 상태`, `면접관 기술`, `플레이어 행동`, `피드백`은 분리해서 보이게 한다.
- 면접 연습에 도움이 되는 정보가 개그보다 우선한다.

## 하네스 규칙
- 서브에이전트 산출물은 감으로 고르지 않는다.
- 먼저 하드 게이트를 통과시킨다:
  1. 타입체크 통과
  2. 빌드 통과
  3. contract 위반 없음
  4. 핵심 플로우 완주 가능
- 그다음 UX, 배틀 감성, 구조 품질을 비교한다.
- 점수만 남기지 말고 항상 아래 3가지를 적는다:
  - 문제
  - 왜 문제인지
  - 수정 방향
- 전체 앱을 한 번에 경쟁시키지 말고 intro, turn panel, result panel 같은 단위로 비교한다.

## 검증 순서
1. 변경한 함수 / evaluator / reducer
2. 관련 컴포넌트
3. first playable 화면 흐름
4. 시나리오 데이터 계약
5. 전체 build check

## 금지사항
- 실제 포켓몬 자산, 공식 배경, 공식 음악 복제 금지
- 설명 없는 점수, 근거 없는 피드백, 하드코딩된 시나리오 분기 금지
- debug print, 절대경로, 비밀키 하드코딩 금지
- approval 없이 사용자 경험 구조를 조용히 바꾸지 않는다.
- “일단 돌아가게”를 이유로 상태 전이를 암묵적으로 처리하지 않는다.

## 기본 질문
항상 현재 변경이 아래 질문에 답해야 한다.
`이 변경이 first playable을 더 또렷하고, 더 검증 가능하고, 더 면접 연습답게 만드는가?`
